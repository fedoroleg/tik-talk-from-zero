import { accountsFeature } from './accounts.reducer';

export const {
  selectAccountsState,
  selectMe,
  selectAccounts,
  selectAccountsFilters,
  selectSubscribers,
  selectAccount
} = accountsFeature;
